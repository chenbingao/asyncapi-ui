{
  "name": "asyncapi-ui-preview",
  "version": "0.1.0",
  "private": true,
  "description": "Swagger UI style AsyncAPI preview service with document switching.",
  "scripts": {
    "build": "node build.mjs",
    "start": "npm run build && node server.js",
    "serve": "node server.js"
  },
  "dependencies": {
    "@asyncapi/react-component": "^3.1.0"
  }
}
